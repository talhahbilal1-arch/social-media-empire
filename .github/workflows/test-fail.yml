name: test-fail
on:
  workflow_dispatch:
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Intentional failure
        run: |
          echo "This job fails on purpose to test auto-heal"
          exit 1
