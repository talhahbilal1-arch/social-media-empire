# ARCHIVED 2026-02-13: Superseded by content-engine.yml
# This ran agents.video_factory daily at 8AM UTC to create videos.
# content-engine.yml now handles the full pin creation + posting pipeline 3x/day
# including content generation, image selection, and multi-platform posting.
# This workflow produced no visible output and was consuming CI minutes.
#
# Agent 2: Video Factory
# Runs daily at 8:00 AM UTC to create videos

name: Video Factory (ARCHIVED)

on:
  # schedule disabled — archived
  # - cron: '0 8 * * *'
  workflow_dispatch:

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
  CREATOMATE_API_KEY: ${{ secrets.CREATOMATE_API_KEY }}

jobs:
  create-videos:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: python -m agents.video_factory
        env:
          GITHUB_RUN_ID: ${{ github.run_id }}
