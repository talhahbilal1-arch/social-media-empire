"""Direct social-media posting suite (X / LinkedIn / Bluesky / Threads).

Replaces the Late API integration with first-party platform clients so the
posting pipeline no longer depends on a third-party broker that can return 401.
"""

__version__ = "0.1.0"
